.gitignore############################
# Terraform
############################
**/.terraform/*
**/.terraform.lock.hcl

# State files (НЕ коммитить)
**/*.tfstate
**/*.tfstate.*

# Crash logs
crash.log
crash.*.log

# CLI config / credentials
.terraformrc
terraform.rc
**/*.tfrc

# Local override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Variables (если содержат секреты)
**/*.tfvars
**/*.tfvars.json

# Plan output
**/*.tfplan

# Provider/plugin binaries (на всякий случай)
**/.terraform-provider*

############################
# Helm / Kubernetes
############################
# Helm deps (helm dependency build)
**/charts/**/charts/
**/charts/**/Chart.lock

# Rendered manifests / output
**/*.rendered.yaml
**/*.rendered.yml
**/*.manifest.yaml
**/*.manifest.yml

# Kubeconfig (если вдруг лежит в проекте)
kubeconfig
**/kubeconfig*
**/.kube/*

############################
# Logs
############################
**/*.log

############################
# OS / IDE
############################
# Windows
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# VS Code
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json

# JetBrains
.idea/
*.iml

# Visual Studio
.vs/
bin/
obj/

############################
# Secrets / env
############################
.env
.env.*
**/*.pem
**/*.key
**/*.pfx
**/*.p12
**/*secret*
**/*secrets*
